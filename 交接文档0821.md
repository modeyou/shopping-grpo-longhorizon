
# Shopping-GRPO 多轮 Harness 修复交接

## 1. 当前目标

继续修复多轮 GRPO Harness 在真实 veRL 0.8 环境下暴露的测试兼容问题，然后：

1. 本地修改、测试并提交。
2. 用户推送 Git。
3. 服务器拉取。
4. 应用 veRL 动态采样补丁。
5. 跑真实 veRL 适配层测试。
6. 再进行单轨迹、同题四轨迹和一次 optimizer update 冒烟。
7. Harness 验收后进行 Qwen3.5-2B 基座模型开发集评测。

不要启动正式 GRPO 长训练，不要使用正式评测集调参。

## 2. 仓库信息

本机仓库：

`D:\shopping-grpo-longhorizon`

服务器仓库：

`/home/gjx/shopping-grpo`

分支：

`feat/multiturn-clarification-agent`

当前远端/服务器 HEAD：

`5278260 feat: add multiturn GRPO harness`

新对话必须将 `D:\shopping-grpo-longhorizon` 作为 workspace root，先运行：

```powershell
git status --short --branch
git log -3 --oneline
```

当前旧对话没有成功修改任何文件。

## 3. 已完成的重要工作

### Reward 与评测

- Reward V4 已实现并冻结。
- `data/environment-v4.json` 已冻结。
- Reward V4 开发集、正式集各 500 题。
- 两套评测集的 gap/complete openings 均已冻结。
- 正式评测集必须保持 sealed，不得用于调试和调参。
- 开发集可用于参数选择和故障分析。

### 教师数据

服务器已采集：

- complete：2083 accepted
- composite：1619 accepted
- autonomous：914 accepted

数据已经足够，采集进程已停止；后续需要时可以继续。

### 多轮 Harness v1

提交 `5278260` 已实现：

- veRL `ask_shopper` 路由；
- `ask_shopper` 不进入 ShopSimulator；
- 问题次数与购物步骤分别计数；
- trajectory-local Shopper `ContextVar`；
- gap/complete opening 初始化；
- `source_goal_hash` 校验；
- 省略事实私有隔离；
- `clarified_constraints` 注入后续 Observation；
- Tool Schema 从统一 Python 定义生成；
- Reward V4 默认配置；
- 多轮 opening → veRL parquet 构建脚本；
- 训练集与开发/正式评测集排除检查。

主要文件：

- `src/shopping_grpo/training/grpo/adapter/agent_loop.py`
- `src/shopping_grpo/training/grpo/adapter/session.py`
- `src/shopping_grpo/training/grpo/adapter/shopper.py`
- `src/shopping_grpo/training/grpo/adapter/tools.py`
- `src/shopping_grpo/training/grpo/adapter/runtime.py`
- `scripts/prepare_multiturn_grpo_dataset.py`
- `scripts/generate_grpo_tool_config.py`
- `scripts/apply_verl_dynamic_sampling_patch.py`
- `tests/test_multiturn_grpo_harness.py`
- `tests/test_verl_adapter.py`
- `tests/test_verl_dynamic_sampling_patch.py`

## 4. 数据层冒烟结果

服务器创建：

`outputs/grpo-harness-smoke-01`

任务：

- 20629
- 2372

构建结果：

```text
rows=4
modes={'gap': 2, 'complete': 2}
private_fact_leak=False
```

Parquet：

`outputs/grpo-harness-smoke-01/multiturn-smoke.parquet`

说明：

- 两个任务来自 `grpo_train.jsonl`；
- 与开发集、正式评测集无重叠；
- gap Prompt 未泄露 omitted facts；
- Harness version 为 `shopping-multiturn-harness-v1`。

## 5. 服务器环境

数据环境：

`/home/gjx/.venvs/grpo-data`

GRPO 环境：

`/home/gjx/.venvs/shopping-grpo`

GRPO 环境版本：

```text
verl            0.8.0
vllm            0.25.1
torch           2.11.0+cu130
transformers    5.15.0.dev0
ray             2.56.1
tensordict      0.10.0
numpy           2.2.6
pyarrow         25.0.1
CUDA runtime    13.0
CUDA available  True
GPU count       5
```

环境安装成功，不要重装。

## 6. GPU 状态与用户要求

服务器有 5 张 RTX 4090。

当前：

- Qwen3.8-27B vLLM 使用 GPU 0–3；
- ComfyUI 大约占用 GPU 4；
- 用户明确不希望停止 ComfyUI；
- 到真实 veRL GPU 冒烟时，可以停止 Qwen3.8，释放 GPU 0–3；
- GRPO rollout 的 Shopper 计划使用 DeepSeek API，不依赖本地 Qwen3.8；
- Qwen3.5-2B 权重位置尚未确认。

在代码测试修复前，不要停止任何服务。

## 7. 当前 9 个失败

服务器执行：

```bash
/home/gjx/.venvs/shopping-grpo/bin/python -m pytest \
  tests/test_multiturn_grpo_harness.py \
  tests/test_verl_adapter.py \
  tests/test_verl_dynamic_sampling_patch.py \
  -q
```

结果：

```text
9 failed, 20 passed
```

这些失败分成三类。

### 7.1 补丁脚本路径写死——真实代码问题

错误：

```text
verl.__file__ is not from the project environment:
  /home/gjx/.venvs/shopping-grpo/lib/python3.12/site-packages/verl/__init__.py
```

原因：

`scripts/apply_verl_dynamic_sampling_patch.py` 把允许环境写死为：

```python
(PROJECT_ROOT / ".venv").resolve()
```

应改为当前运行 Python 的环境：

```python
import sys

expected_environment = Path(sys.prefix).resolve()
```

仍需保留：

- veRL 版本必须等于 0.8.0；
- `verl.__file__` 必须位于当前解释器 `sys.prefix` 内；
- 已知 SHA-256 校验；
- 补丁幂等性；
- 未知文件哈希必须拒绝。

这不是降低安全校验，而是允许仓库内 `.venv` 和
`~/.venvs/shopping-grpo` 两种隔离布局。

### 7.2 新测试在真实 veRL 下传入 dict Schema——测试问题

失败：

```text
AttributeError: 'dict' object has no attribute 'function'
```

位置：

`tests/test_multiturn_grpo_harness.py::make_ask_tool`

无 veRL 环境下可以传字典，但安装真实 veRL 后必须像
`tests/test_verl_adapter.py::make_tool` 一样转换：

```python
try:
    from verl.tools.schemas import OpenAIFunctionToolSchema
except ImportError:
    tool_schema = schema
else:
    tool_schema = OpenAIFunctionToolSchema.model_validate(schema)

return ShopSimulatorTool({}, tool_schema)
```

不要修改生产 `ShopSimulatorTool` 来接受错误类型；只修测试 helper。

### 7.3 `test_verl_adapter.py` 中的旧 Reward/Guard 断言

这些测试仍对应旧 Reward 组件契约，不能为了通过测试恢复旧生产逻辑。

需要更新：

#### Guard reason

当前实际原因：

```text
click_not_in_previous_observation
```

旧测试期待：

```text
asin_not_visible
```

以当前 `action_reject_reason()` 为准，更新测试断言。

#### runtime `reward_detail`

旧测试要求 runtime 里不存在 `reward_detail`。

现在 runtime 初始化为：

```python
"reward_detail": None
```

终局后只保存经过 `validate_reward()` 最小化的公开 Reward V3/V4 诊断。

测试应改成：

```python
assert state["reward_detail"] is None
assert "goal" not in state
assert "instruction_full" not in str(state)
assert "omitted_facts" not in str(state)
```

不能删除生产 `reward_detail`。

#### `reward_components`

旧字段 `reward_components` 已废弃。

这类断言：

```python
state["reward_components"]
```

应改为检查：

```python
state["reward_detail"]
state["reward_version"]
state["reward_type"]
state["reward_valid"]
```

无效或缺少版本的终局 Reward 应有：

```python
state["reward_detail"] is None
state["infrastructure_invalid"] is True
```

#### 旧的四组件 Reward fixture

旧测试只返回：

```python
{
    "r_type": 1,
    "r_att": 1,
    "r_option": 0.5,
    "r_price": 1
}
```

当前严格 Harness 必须拒绝这种无版本 Reward。

如果测试目标是“合法 Reward 不泄露隐藏字段”，应将 fixture 改成完整、
有效的 Reward V4，例如包含：

```python
{
    "reward_version": "shopsimulator-reward-v4",
    "reward_type": "valid_alternative_purchase",
    "reward_valid": True,
    "termination_reason": "valid_alternative_purchase",
    "target_asin_match": False,
    "terminal_utility": 0.6,
    "purchase_success": True,
    "sampling_invalid": False,
    "weighted_score": 0.6,
    "evidence_coverage": 1.0,
    "dimension_scores": {
        "brand": 0.0,
        "model": 0.0,
        "core_functions": 1.0,
        "key_options": 0.5
    },
    "constraint_scores": {},
    "hard_gates": {
        "category": {
            "status": "pass",
            "passed": True,
            "verifiable": True,
            "comparator": "category_leaf_ancestor_chain",
            "source_field": "category"
        }
    },
    "hidden_answer": "do not retain"
}
```

注意终局 `reward` 必须与 `terminal_utility` 相同。

然后断言：

- `infrastructure_invalid is False`
- 保存的 `state["reward_detail"]` 不含 `hidden_answer`
- `weighted_score` 等公开字段正确。

#### unsupported Reward

旧测试期待：

```text
unsupported-reward
```

不会成为 infrastructure invalid。

当前严格策略恰好相反：unsupported Reward 必须将轨迹判为基础设施无效，
否则训练可能吃到未知奖励协议。

更新测试为断言：

```python
state["infrastructure_invalid"] is True
state["reward_detail"] is None
state["error"].startswith("invalid_terminal_reward_detail:")
```

不要放宽生产 Reward 验证。

## 8. 本机修复后测试顺序

先在无 veRL 的本机运行：

```powershell
$env:PYTHONPATH = "./src"

python -m pytest `
  tests/test_multiturn_grpo_harness.py `
  tests/test_multiturn_clarification.py `
  tests/test_multiturn_evaluation.py `
  tests/test_public_entrypoints.py `
  tests/test_environment_manifest.py `
  -q

python scripts/generate_grpo_tool_config.py --check
git diff --check
```

本机可能没有 veRL，因此 `test_verl_adapter.py` 和动态补丁测试需要服务器复测。

提交建议：

```text
fix: align veRL harness tests and environment discovery
```

提交后让用户推送。

## 9. 服务器拉取后的命令

```bash
cd ~/shopping-grpo

git pull --ff-only origin feat/multiturn-clarification-agent
git log -3 --oneline
git status --short --branch

export PYTHONPATH=./src
GRPO_PYTHON="/home/gjx/.venvs/shopping-grpo/bin/python"

"$GRPO_PYTHON" scripts/apply_verl_dynamic_sampling_patch.py

"$GRPO_PYTHON" -m pytest \
  tests/test_multiturn_grpo_harness.py \
  tests/test_verl_adapter.py \
  tests/test_verl_dynamic_sampling_patch.py \
  -q
```

预期：

- 补丁成功应用；
- 29 tests passed；
- 再次运行补丁应报告 already applied 或等价的幂等结果；
- 不应再出现 `.venv` 路径拒绝。

服务器有两个历史无效 manifest 目录处于 untracked：

```text
data/multiturn/evaluation-dev-v2-invalid-manifest-b472c8/
data/multiturn/evaluation-v2-invalid-manifest-b472c8/
```

不要把它们加入提交，也不要未经确认删除。

## 10. 修复通过后的下一阶段

确认 Qwen3.5-2B 权重位置，然后：

1. 停止 Qwen3.8 vLLM，释放 GPU 0–3；
2. 保留 GPU 4 上的 ComfyUI；
3. 使用 DeepSeek API 作为 GRPO Shopper；
4. 用 `outputs/grpo-harness-smoke-01/multiturn-smoke.parquet` 做一次可丢弃冒烟；
5. 先单轨迹；
6. 再同题 `n=4`；
7. 检查四条 ContextVar、ShopSimulator session、Shopper history 不串线；
8. 再进行一次 optimizer update；
9. 丢弃 smoke checkpoint；
10. Harness 验收后开始 Qwen3.5-2B 基座开发集评测。

不要在 smoke 阶段使用正式评测集。


---

# 项目后续工作路线

## 当前项目所处阶段

目前已经基本完成：

- Reward V4 的设计、实现和环境冻结
- 开发集、正式评测集及对应 opening 的冻结
- complete、composite、autonomous 三类教师轨迹采集
- 多轮 Shopping Agent Harness v1
- `ask_shopper`、ShopSimulator 工具调用、上下文投影和 Action Guard
- 多轮 GRPO 数据集转换
- veRL 接入的主体代码

当前正在处理的是：让 Harness 与服务器上的真实 `veRL==0.8.0` 完全兼容，并通过端到端 smoke test。

整个项目还没有进入正式 SFT 和 GRPO 训练阶段。

---

## 阶段一：完成真实 veRL Harness 验收

先修复当前 9 个测试，并在服务器上完成以下验证：

1. veRL dynamic-sampling patch 可以正确应用。
2. patch 重复运行具有幂等性。
3. `ask_shopper` 在真实 veRL ToolAgentLoop 中可调用。
4. shopper 问答不消耗 ShopSimulator step。
5. 每条 GRPO 轨迹具有独立的：

   - ShopSimulator session
   - shopper 状态
   - action history
   - clarified constraints
   - reward 和诊断信息

6. Reward V4 只在终止后进入训练侧，不泄露给 Actor。
7. 隐藏目标和 shopper 私有事实不进入 Actor 上下文。
8. Observation Projection 和 Action Guard 在真实 rollout 中生效。

然后按难度逐步运行：

- 单题、单轨迹 rollout
- 单题、4 条并行轨迹
- 多题 rollout
- 一次可丢弃的 optimizer update

通过标准：

- 没有私有事实泄露
- 没有 session 串扰
- Reward V4 正确返回
- 4 条轨迹可以独立结束
- response mask、token、log probability 对齐
- 没有 OOM
- 没有无限循环
- 可以真正执行一次参数更新

完成后才能认为“多轮 GRPO Harness 已经落地”。

---

## 阶段二：确定训练和评测模型配置

### Actor 基座模型

当前计划使用：

```text
Qwen3.5-2B
```

需要在服务器上确认：

- 模型的准确仓库名或本地路径
- 是否为 Base 还是 Instruct 版本
- chat template
- tool-call 格式
- 最大上下文长度
- bf16 支持情况
- 是否默认开启 thinking
- veRL/vLLM 是否能稳定加载

建议首先使用 Instruct 版本完成工程链路，因为它更容易形成有效工具调用。若项目目标强调“从弱基座学习 Agent 能力”，可以再补充 Base 版本对照。

### Shopper

正式开发评测和 GRPO rollout 推荐：

```text
DeepSeek API
```

Shopper 只模拟用户回答，不承担选商品或执行环境操作。

需要固定：

- 模型版本
- temperature
- max tokens
- prompt 版本
- 超时和重试策略
- 随机种子或可复现设置
- 无效回答的处理方式

### Rubric 与 Judge

当前建议：

- Rubric/约束抽取：本地 Qwen3.8-27B
- 语义 Judge：DeepSeek-v4-flash-0731

但确定正式方案前，应在开发集抽样约 50–100 条做人工核对，检查：

- Rubric 是否漏掉或错误拆分约束
- Judge 是否偏好长答案
- 对同一轨迹重复评分是否稳定
- Qwen Rubric 与 DeepSeek Judge 是否经常矛盾
- 中文商品属性、规格和价格判断是否可靠

固定后要记录完整模型名、提示词版本和推理参数。

---

## 阶段三：运行基座模型开发集评测

在任何训练前，对未训练的 Qwen3.5-2B 做基线评测。

只使用：

```text
data/multiturn/evaluation-dev-v2
```

不要提前使用正式集调参。

每个任务包含三种条件：

1. `complete`：用户一次性给出完整需求
2. `gap + shopper`：允许 Actor 主动澄清
3. `gap + no shopper/受控条件`：观察缺失信息的实际影响

建议先跑小规模：

- 10 题：检查执行链路
- 50 题：排查主要失败类型
- 500 题：形成正式开发集基线

评测至少记录以下五个面板：

### 1. 最终任务结果

- Reward V4
- gold/valid alternative/wrong purchase
- purchase success
- 任务完成率

### 2. 澄清行为

- 是否需要澄清
- 是否发起澄清
- 问题是否覆盖缺失维度
- shopper 回答利用率
- 过度澄清率
- 未澄清率

### 3. Agent 过程质量

- 搜索次数
- 打开商品次数
- 查看详情、属性、规格次数
- 总 step 数
- 重复动作率
- 无效工具调用率
- Action Guard 拒绝率
- repeat loop 率

### 4. 上下文和系统效率

- 原始 Observation tokens
- 投影后 tokens
- 压缩比例
- 最大上下文长度
- context compaction 次数
- 超时、OOM、API 错误
- 单题耗时和 API 调用量

### 5. 语义质量

- 问题相关性
- 信息增益
- 是否泄露隐藏答案
- 是否正确使用澄清信息
- 最终购买理由是否与证据一致

基座评测结果是后续判断 SFT 和 GRPO 是否真正提升的参照点。

---

## 阶段四：整理最终 SFT 数据集

已有三类接受数据：

- complete：2083 条
- composite：1619 条
- autonomous：914 条

不能简单全部拼接。需要先做数据审计和配比。

### 建议的数据作用

- complete：学习基本搜索、核验和购买能力
- composite：学习规范、可靠的一问一答澄清流程
- autonomous：学习何时主动询问，以及如何继续完成任务

### 建议初版配比

可以先构造约 2000–3000 条训练集，例如：

- complete：40%
- composite：35%
- autonomous：25%

具体数量应根据去重和长度分布调整，而不是机械采样。

### 合并前检查

- 按 `task_id` 或 `source_goal_hash` 去重
- 排除开发集和正式评测集
- 检查三类数据是否出现同源任务跨 split 泄露
- 检查 Reward V4 可达性
- 检查隐藏事实是否进入不应出现的位置
- 检查工具调用 schema
- 检查终止状态和购买动作
- 检查 trajectory token 长度
- 检查商品类别覆盖
- 检查缺失维度类型分布
- 检查澄清问题数量分布
- 对高频模板和高度相似轨迹降采样

需要生成一份数据集 manifest，至少记录：

- 输入文件和 SHA256
- 筛选规则
- 随机种子
- 每种来源数量
- train/validation 数量
- 去重和排除统计
- schema 版本
- reward 版本

---

## 阶段五：SFT 训练

建议先完成 SFT，再进入 GRPO。Qwen3.5-2B 在 4×4090 上具有足够训练空间。

### 推荐起步方案

优先考虑：

- QLoRA 或 LoRA
- bf16
- gradient checkpointing
- Flash Attention
- sequence packing
- 按 token 数而不是样本数控制 batch
- 保存 optimizer、scheduler 和数据 manifest

先做小规模 smoke：

1. 取 32–64 条数据。
2. 训练少量 steps。
3. 确认 loss 正常下降。
4. 保存并重新加载 adapter/checkpoint。
5. 用模型完成一次真实 ShopSimulator 轨迹。
6. 确认工具调用格式没有被训练破坏。

再进行正式训练。

### SFT 需要比较的模型

至少保留：

- Base：未训练 Qwen3.5-2B
- SFT-complete：仅完整请求数据
- SFT-multiturn：三类混合数据

这样才能判断多轮数据是否真的带来澄清能力，而不仅是总体训练量增加。

---

## 阶段六：SFT 后开发集评测

使用与基座完全相同的：

- 开发任务
- opening
- shopper
- Reward V4
- Judge/Rubric
- Harness 配置
- 最大步数
- 采样参数

重点观察：

- complete 条件是否提升
- gap 条件是否提升更多
- 主动澄清率是否合理
- 澄清后购买成功率是否提升
- 是否出现“每题都问”的过度澄清
- 是否牺牲搜索和购买能力
- 无效工具调用和 repeat loop 是否下降
- 上下文长度和平均步数是否恶化

如果 SFT 后不能稳定完成工具调用，不应该直接进入正式 GRPO，应先修正数据格式或训练配置。

---

## 阶段七：构造正式 GRPO 数据集

GRPO 数据来自训练任务，不使用评测集。

应准备两种训练条件：

- `complete`
- `gap + shopper`

可以混合训练，也可以分阶段训练。

建议初版：

```text
complete : gap = 1 : 1
```

每个 prompt 同时采样 4 条独立轨迹，用组内相对 Reward 计算 advantage。

需要明确：

- group size：4
- 每条轨迹最大 shop steps
- 最多 shopper 问题数
- rollout temperature
- Actor 最大生成 tokens
- context projection 预算
- 无效轨迹是否排除
- reward_unverifiable 是否排除
- infrastructure failure 是否重采样
- 全同 reward group 是否跳过
- dynamic sampling 规则

---

## 阶段八：设计 GRPO Reward

Reward V4 是任务结果评估器，但 GRPO 训练可以在其基础上加入轻量过程项。

建议不要立即引入复杂 PRM。第一版采用可解释的规则型组合：

```text
R_total =
    R_v4_terminal
  + R_clarification
  - P_invalid_action
  - P_repeat_loop
  - P_excessive_questions
  - P_timeout
```

其中：

- `R_v4_terminal`：最终购买质量的主要奖励
- `R_clarification`：仅在确有信息缺口且询问有效时奖励
- `P_invalid_action`：无效工具调用惩罚
- `P_repeat_loop`：重复循环惩罚
- `P_excessive_questions`：过度询问惩罚
- `P_timeout`：超时或未完成惩罚

原则：

- 最终任务成功仍是主信号
- 过程奖励不能盖过最终购买结果
- complete 条件不应奖励无意义提问
- gap 条件不能只因“问了问题”就奖励
- 基础设施错误不应当成模型负奖励
- reward_unverifiable 应与错误购买分开处理

PRM 或 learned step-level reward 可以作为后续增强，不是第一版必须项。先证明规则型 Reward 能带来稳定改进。

---

## 阶段九：GRPO smoke 和正式训练

### Smoke 1：单轨迹

确认：

- Actor 能调用工具
- shopper 能回答
- 环境能结束
- Reward V4 能返回
- 隐藏信息不泄露

### Smoke 2：同题 4 轨迹

确认：

- 4 个 session 互不串扰
- shopper 私有状态独立
- reward 分布存在差异
- advantage 可正常计算
- 没有全零或全同导致的异常

### Smoke 3：一次 optimizer update

确认：

- rollout token 与 log probability 对齐
- response mask 正确
- backward 成功
- optimizer step 成功
- checkpoint 可以保存
- 显存峰值可接受

Smoke checkpoint 不用于正式结果。

### 正式训练

先跑短实验，例如：

- 少量训练任务
- 50–100 个 optimizer steps
- 定期跑开发集小样本
- 观察 KL、entropy、reward、长度和成功率

确认稳定后再扩大训练。

必须监控：

- 平均 Reward V4
- group reward variance
- KL divergence
- policy entropy
- response/trajectory 长度
- 有效 group 比例
- infrastructure invalid 比例
- reward_unverifiable 比例
- shopper 调用次数
- Action Guard 拒绝率
- repeat loop 率
- GPU 显存和吞吐

---

## 阶段十：GRPO 后开发集评测

比较至少以下模型：

1. Qwen3.5-2B Base
2. SFT 模型
3. SFT + GRPO 模型

最好再加入：

4. 仅 complete SFT
5. 无澄清奖励的 GRPO
6. 无上下文投影或不同投影预算的版本

主要问题不是“Reward 是否提高”，而是：

- GRPO 是否提高真实购买成功率
- 是否更会判断何时需要澄清
- 是否正确使用 shopper 回答
- 是否降低重复搜索和无效操作
- 是否产生 reward hacking
- 是否在 complete 条件下过度提问
- 是否牺牲任务效率换取奖励

开发集用于选择 checkpoint 和超参数。

---

## 阶段十一：正式评测

只有所有方法和配置确定后，才运行：

```text
data/multiturn/evaluation-v2
```

正式评测集不能用于：

- 调 reward 权重
- 改 prompt
- 选 checkpoint
- 调 temperature
- 修改 shopper
- 修改 Judge/Rubric
- 改 Harness 规则

正式评测至少报告：

| 模型 | Complete | Gap+Shopper | Gap受控条件 | 澄清有效率 | 购买成功率 | Reward V4 |
|---|---:|---:|---:|---:|---:|---:|
| Base | | | | | | |
| SFT | | | | | | |
| SFT+GRPO | | | | | | |

同时报告效率、错误和上下文面板，而不仅是平均 Reward。

如果算力允许，每个条件可使用多个随机种子，并报告均值与置信区间。

---

## 阶段十二：必要消融实验

为了说明各模块是否真正有价值，推荐最少完成以下消融：

### 数据消融

- complete-only SFT
- complete + composite
- complete + composite + autonomous

回答：三类数据分别贡献了什么？

### 训练消融

- Base
- SFT
- SFT + GRPO

回答：SFT 和 Agentic RL 各自贡献了什么？

### 澄清消融

- 不允许询问 shopper
- 允许询问但没有澄清过程奖励
- 完整多轮方案

回答：提升是否真的来自澄清能力？

### Harness 消融

- 无 Observation Projection 或较大预算
- 当前页面类型投影
- 不同 token budget

回答：上下文工程是否降低成本且不损害成功率？

### Reward 消融

- 仅终局 Reward V4
- Reward V4 + 轻量过程奖励

回答：step-level shaping 是否必要？

不建议一次做太多实验，先完成能回答核心项目问题的最小组合。

---

## 阶段十三：结果分析和错误分类

需要对失败轨迹进行结构化分类：

- 没有意识到需要澄清
- 问了不相关的问题
- 问题覆盖不全
- 没有使用 shopper 回答
- 搜索 query 不合理
- 打开错误商品
- 没有核验关键属性
- 规格选择错误
- 价格判断错误
- 过早购买
- 过度搜索
- 重复循环
- 无效工具调用
- 上下文投影丢失必要信息
- 环境或 API 故障
- Reward 无法验证

每类保留典型案例，既用于改进项目，也用于最终报告。

---

## 阶段十四：工程固化和项目交付

最后需要补齐：

- 一键环境安装说明
- 数据生成和冻结命令
- SFT 数据构建命令
- SFT 训练命令
- GRPO 数据构建命令
- GRPO 训练命令
- 开发集评测命令
- 正式评测命令
- 模型与数据 manifest
- checkpoint 命名规范
- 实验配置和随机种子
- 结果汇总脚本
- 故障恢复说明
- GPU 部署方案

建议最终形成以下目录：

```text
configs/
  sft/
  grpo/
  evaluation/

data/
  sft/
  grpo/
  multiturn/evaluation-dev-v2/
  multiturn/evaluation-v2/

docs/
  data-collection.md
  multiturn-teacher-sft-review.md
  reward-v4.md
  multiturn-evaluation.md
  agent-harness-context-engineering.md
  training-and-experiment-plan.md

outputs/
  baselines/
  sft/
  grpo/
  evaluation/
```

模型、训练数据、环境数据和评测集都应以 hash 或 manifest 绑定，避免之后无法复现实验。

---

# 推荐的实际执行顺序

后续新会话应严格按照以下顺序推进：

1. 修复当前 9 个 veRL/Harness 测试。
2. 完成真实 veRL 单轨迹、4 轨迹和一次更新 smoke。
3. 确认 Qwen3.5-2B 模型版本与路径。
4. 运行基座模型开发集评测。
5. 审计并冻结最终 SFT 混合数据集。
6. 完成 SFT smoke。
7. 正式 SFT。
8. 运行 SFT 开发集评测。
9. 冻结 GRPO 数据和 Reward 配置。
10. 完成 GRPO smoke。
11. 正式 GRPO。
12. 运行 GRPO 开发集评测并选 checkpoint。
13. 完成必要消融实验。
14. 所有方案固定后，仅运行一次正式评测集。
15. 汇总结果、典型案例和工程文档。

最重要的近期里程碑不是立即开始大规模训练，而是：

```text
真实 veRL Harness 通过
→ 基座模型开发集结果得到
→ SFT 数据集冻结
→ SFT 开始
```

这三个节点完成后，项目就会从“基础设施建设阶段”正式进入“训练和实验阶段”。